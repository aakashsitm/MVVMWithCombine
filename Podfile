# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EliStore' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EliStore
  pod 'Mocker', '~> 2.5.4'

  target 'EliStoreTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EliStoreUITests' do
    # Pods for testing
  end

end
